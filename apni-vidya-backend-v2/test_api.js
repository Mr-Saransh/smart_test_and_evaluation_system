require("dotenv").config(); const { Pool } = require("pg"); const pool = new Pool({ connectionString: process.env.DATABASE_URL }); const jwt = require("jsonwebtoken"); const http = require("http"); async function test() { const users = await pool.query("SELECT * FROM users WHERE role='institute_admin' LIMIT 1"); if(users.rows.length === 0) return console.log("no admin user"); const user = users.rows[0]; const token = jwt.sign({ id: user.id, role: user.role, full_name: user.full_name }, process.env.JWT_SECRET); console.log("Token generated"); http.get("http://localhost:3000/api/courses/4b4005ab-0baa-4e1f-a664-7dd269a879fb", { headers: { Authorization: "Bearer " + token } }, (res) => { console.log("Status:", res.statusCode); res.on("data", d => console.log(d.toString())); }); } test().finally(()=>pool.end());
