require("dotenv").config(); const { Pool } = require("pg"); const pool = new Pool({ connectionString: process.env.DATABASE_URL }); async function run() { try { const res = await pool.query("SELECT c.*, b.name AS batch_name FROM courses c LEFT JOIN batches b ON c.batch_id = b.id WHERE c.institute_id = $1 AND c.is_active = true ORDER BY c.created_at DESC", ["4b4005ab-0baa-4e1f-a664-7dd269a879fb"]); console.log(res.rows); } catch(e) { console.error(e); } finally { pool.end(); } } run();
